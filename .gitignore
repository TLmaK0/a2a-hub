# Secretos y entorno — NUNCA se commitean
.env
*.env
!.env.example
local/
secrets/
*.token

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.ruff_cache/

# Datos locales (task store SQLite de desarrollo)
*.db
*.sqlite
*.sqlite3

# Agent scratch, incl. per-task worktrees. The whole dir: an always-dirty repo
# makes the fleet's at-risk-work detector (git status based) stop warning.
.claude/
